<odoo>

    <record id="view_hr_payslip_form" model="ir.ui.view">
        <field name="name">hr.payslip.form.inherit.rrhh</field>
        <field name="model">hr.payslip</field>
        <field name="inherit_id" ref="hr_payroll.view_hr_payslip_form"/>
        <field name="arch" type="xml">
            <field name="company_id" position="after">
                <field name="porcentaje_prestamo"/>
                <field name="descuento_isr"/>
                <field name="cuenta_analitica_id"/>
                <!--<field name="etiqueta_empleado_ids" widget="many2many_tags"/>-->
            </field>
            <!--
            <button name="action_payslip_cancel" position="attributes">
                <attribute name="invisible">state not in ('draft','done','verify','paid')</attribute>
            </button>
            -->
        </field>
    </record>

</odoo>
